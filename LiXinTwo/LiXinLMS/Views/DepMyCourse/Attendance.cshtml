@{
    ViewBag.Title = "Attendance";
    Layout = null;
    var Cl_Attendce = ViewBag.Cl_Attendce as LiXinModels.DepAttendce.Dep_Attendce;
}

<div class="main-c">
            @*@(Html.Action("SiteMapLink", "Common", new { linkName = "课程详情" }))
            @Html.Action("MyCourse", "MyCourse", new { id = @ViewBag.Id })*@
            
            <table class="tab-Form">
                

                 @if (ViewBag.Cl_Attendce == null && ViewBag.backFrom==0)
                { 
                <tr><td>
                        <div class="alert alert-info" >                        
                            <i></i>
                            <strong>提醒</strong>
                            等待考勤信息上传中。
                        </div>
                    </td></tr>
                 }                
                <tr>                  
                            @if (Cl_Attendce!=null)
                            {
                           <strong>考勤状态:@Cl_Attendce.StatusStr</strong>
                            }
                            else
                            {<strong>考勤状态:无</strong>
                                    }
                    </tr>
            </table>
    <center class="mt10">
       
            @Html.ActionLink("返回", "MyCourseList", "DepMyCourse", new { tp=1 }, new { @class = "btn btn-cancel" })
        
    </center>
        </div>